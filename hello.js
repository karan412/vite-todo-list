/*

ternary 
let age = 89;
let name = age < 80 ? "Gman": "HeMan";
console.log(name);
*/
//------------------------------------
/*

// Objs 
// Destructering objects
const person = {
    name : "Kan",
    age : 30,
    married : false,
};

//normally

const name = person.name;
const age = person.age;
const isMarried = person.married;


//dest
const {name, age, married} = person;

//copy obj with filters (manipulating objs in a state)
const peron2 = {...person, name:"Jan"};
*/
//------------------------------------

/*
let names = ["jack", "jill", "sam"];
 
//Map and perform operations
names.map((name)=>{
    //op
})
*/

//------------------------------------